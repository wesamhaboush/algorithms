package com.codebreeze.algorithms;

/**
 * algorithm to multiply numbers recursively for all c >= 2.
 *
 * multiply(y, z)
 *      if z = 0,
 *          return 0
 *      else
 *          return multiply(cy, floor(z/c) + y * (z % c))
 */
public class Multiply
{
}
