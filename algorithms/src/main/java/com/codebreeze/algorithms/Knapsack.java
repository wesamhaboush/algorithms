package com.codebreeze.algorithms;

/**
 * given a set of integers S = {s1, s2, s3, ..., sn}, and a target
 * number T, find a subset of S which adds up exactly to T. For example,
 * there exists a subset within S = {1, 2, 5, 9, 10} that adds up to 22 but
 * not 23.
 */
public class Knapsack
{
}
