package com.codebreeze.algorithms;

/**
 * Sum(i = 1, i = n)(i) = n(n+1)/2 for n >= 0
 * Sum(i = 1, i = n)(i^2) = n(n+1)(2n+1)/6 for n >= 0
 * Sum(i = 1, i = n)(i^3) = (n^2)((n+1)^2)/4 for n >=0
 * Sum(i = 1, i = n)(i(i+1)(i+2)) = n(n+1)(n+2)(n+3)/4
 * Sum(i = 0, i = n)(a^i) = (a^(n+1) - 1)/(a - 1)
 * Sum(i = 1, i = n)(1/(i(i+1))) = n/(n+1)
 * n^3 + 2n is divisible by 3 for all n >= 0
 * Sum(i = 1, i = n)(i^3) = (Sum(i = 1, i = n)(i))^2
 * log_a(xy) = log_a x + log_a y
 * log_a x^y = y log_a x
 * log_a x = (log_b x)/(log_b a)
 * x^(log_b y) = y^(log_b x)
 * ceil( lg(n+1) ) = floor( lg n ) + 1
 * number of bits require to represent number n >= 1 -> floor( lg_2 n) + 1 bits
 *
 */
public class Laws
{
}
